# 更新日志

## 0.2.0 — 2026-08-27

### 新增

- 完成 Windows 桌面端的本地 Ollama 聊天闭环；
- 支持 SQLite 本地会话、搜索、重命名、删除和 Markdown/JSON 导出；
- 支持按模型维护别名，并在当前界面即时显示；
- 支持 4K、8K、16K 上下文大小设置；
- 支持快速、平衡、推理三种性能模式；
- 支持思考/正文分离、停止生成、复制和基础 Markdown 渲染；
- 增加流式事件合并、性能指标和 Debug 诊断能力；
- 增加自定义无边框窗口栏和 Windows Debug/Release 构建入口。

### 隐私与边界

- Ollama 地址固定为本机 `127.0.0.1:11434`；
- 不包含账号、云端同步、遥测、联网搜索、Shell、文件执行、MCP 或模型工具调用；
- 思考内容默认不写入本地数据库。

### 已知限制

- 当前仅验证 Windows 10/11；
- 没有完整的桌面 Playwright E2E 流程；
- 当前 Tauri installer bundle 未启用，发行版主要提供源码以及 Debug/Release 可执行文件；
- 未提供安装包签名和自动更新。
