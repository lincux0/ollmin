{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the local Ollama diagnostic window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
